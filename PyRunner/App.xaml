<Application
    x:Class="PyRunner.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- M2 主题体系（Phase C）：语义色 Token 真相源 + 图标库（Path 内联）。
                     界面一律 ThemeResource 取色，禁止再直写色值。 -->
                <ResourceDictionary Source="Assets/Themes/ThemeResources.xaml" />
                <ResourceDictionary Source="Assets/Icons/IconLibrary.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- WinUI 3 系统控件深色强调色对齐品牌主色（评审修复：单一真相源，
                 引用 DarkTheme.xaml 的 AccentColor；两者必须同步修改，不得再直写色值） -->
            <StaticResource x:Key="AccentFillColorDefaultBrush" ResourceKey="AccentBrush" />
            <StaticResource x:Key="AccentFillColorSecondaryBrush" ResourceKey="AccentHoverBrush" />
            <StaticResource x:Key="AccentFillColorTertiaryBrush" ResourceKey="AccentPressedBrush" />
            <StaticResource x:Key="TextOnAccentFillColorPrimaryBrush" ResourceKey="PrimaryButtonForegroundBrush" />

            <!-- 主窗口按钮只保留结构与焦点框，不让 WinUI 默认模板按系统主题重写
                 PointerOver / Pressed / Disabled 颜色。交互颜色由窗口现有指针状态机设置。 -->
            <ControlTemplate x:Key="PyRunnerButtonTemplate" TargetType="Button">
                <Grid>
                    <ContentPresenter
                        x:Name="ContentPresenter"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        ContentTransitions="{TemplateBinding ContentTransitions}"
                        Foreground="{TemplateBinding Foreground}"
                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                        Padding="{TemplateBinding Padding}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                    <Border
                        x:Name="FocusBorder"
                        BorderBrush="{ThemeResource SelectedBorderBrush}"
                        BorderThickness="0"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        IsHitTestVisible="False" />
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="FocusStates">
                            <VisualState x:Name="Unfocused" />
                            <VisualState x:Name="Focused">
                                <VisualState.Setters>
                                    <Setter Target="FocusBorder.BorderThickness" Value="2" />
                                </VisualState.Setters>
                            </VisualState>
                            <!-- Mouse clicks must not leave a persistent focus ring. -->
                            <VisualState x:Name="PointerFocused" />
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                </Grid>
            </ControlTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
